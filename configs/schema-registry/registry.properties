# Дополнительные настройки Schema Registry.
# Основные параметры задаются через env в compose/stack.

# Пример: совместимость схем по умолчанию (BACKWARD/FORWARD/FULL/NONE)
compatibilityLevel=BACKWARD

