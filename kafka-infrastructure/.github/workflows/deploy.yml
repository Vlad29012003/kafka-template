name: Deploy (template)

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Info
        run: |
          echo "Шаблон workflow."
          echo "Обычно здесь настраивают SSH/Secrets и запускают scripts/deploy/deploy-prod.sh на сервере."

