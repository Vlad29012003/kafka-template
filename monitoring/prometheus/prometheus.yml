global:
  scrape_interval: 15s

# Заготовка: чтобы собирать метрики Kafka, обычно добавляют JMX Exporter
# и настраивают scrape на его порт (например, kafka:9101).
scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

